Primary simplistic design, we will decide whether we update it to provide coverage for more options or more businesses,
for now it stays the same

DROP TABLE Users;

DROP TABLE User_favourites;

DROP TABLE Business;

DROP TABLE Hairdresser;

DROP TABLE Restaurants;


CREATE TABLE Users(
id TEXT PRIMARY KEY,
name TEXT NOT NULL,
surname1 TEXT,
surname2 TEXT NOT NULL,
email TEXT NOT NULL)


CREATE TABLE Business(
   id TEXT PRIMARY KEY,
   CIF TEXT NOT NULL,
   type INTEGER NOT NULL,
   latitude REAL NOT NULL,
   longitude REAL NOT NULL,
   schedule TEXT NOT NULL,
   ratings REAL
)

/*the CASCADE update or delete may be a source of errors, check when actually implementing them*/

CREATE TABLE User_favourites(
  id TEXT PRIMARY KEY,
  FOREIGN KEY(b_id)
	REFERENCES Business(id)
		ON UPDATE CASCADE
			ON DELETE CASCADE
  FOREIGN KEY(u_id)
	REFERENCES Users(id)
		ON UPDATE CASCADE
			ON DELETE CASCADE
)



CREATE TABLE Restaurants(
  capacity TEXT NOT NULL,
  menu TEXT NOT NULL, 
  AQUI HAY QUE VER LO DE COMO PONER QUE BUSINESS_ID SEA FOREIGN Y PRIMARY KEY A LA VEZ
  ON UPDATE CASCADE
  ON DELETE CASCADE
)


CREATE TABLE Hairdresser(
 services TEXT NOT NULL,
 AQUI HAY QUE VER LO DE COMO PONER QUE BUSINESS_ID SEA FOREIGN Y PRIMARY KEY A LA VEZ
 ON DELETE CASCADE
 ON UPDATE CASCADE
)








